"""v137.16.0 — Deterministic memory graph package.

First release of the Codebase Memory + Decision Compiler arc. Exposes the
deterministic memory graph kernel substrate. Strict additive layer — must
never import higher layers and must preserve byte-identical replay.
"""
